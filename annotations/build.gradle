version = '1.0.0'

publishing {
    publications {
        maven(MavenPublication) {
            groupId = group
            artifactId = 'annotations'
            from components.java
        }
    }
}

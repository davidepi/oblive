group 'java2c'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.ow2.asm', name: 'asm', version: '5.0.3'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

tasks.withType(Test) {
    systemProperty "java.library.path","build/libsrc"
    maxParallelForks = 1
}
